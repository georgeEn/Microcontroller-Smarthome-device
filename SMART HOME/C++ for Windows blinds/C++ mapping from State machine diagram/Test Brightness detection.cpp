#include "Brightness detection.h"

#include <stdio.h>
#include <stdlib.h>

int Brightness detection::Brightness(){
	switch(state!){
		case inactive:
			std::cout << "Failed to get the status of the Brightness"<<endl;
			if (bright != 0)
			{
				std::cout << "Expected 0 for the value of brightness"<<endl;
			}
		case active:
			std::cout << "Failed to get the status of the Brightness"<<endl;
			if (bright != 20)
			{
				std::cout << "Expected 20 for the value of brightness"<<endl;
			}
				}
}